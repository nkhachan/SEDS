#screens.kv

<PortScreen>:
    BoxLayout:
        spacing: 20
        padding: 20
        Button:
            text: "Vulcan2 Configuration"
            on_press:
                root.manager.transition.direction = 'left'
                root.manager.transition.duration = 1
                root.manager.current = 'screen_two'

<Vulcan2Screen>:
    BoxLayout:
        spacing: 20
        padding: 20
        Button:
            text: "Port Configuration"
            on_press:
                root.manager.transition.direction = 'right'
                root.manager.transition.duration = 1
                root.manager.current = 'screen_one'
        Button:
            text: "Pressure Table Configuration"
            on_press:
                root.manager.transition.direction = 'left'
                root.manager.transition.duration = 1
                root.manager.current = 'screen_three'

<PressureTableScreen>
    BoxLayout:
        spacing: 20
        padding: 20
        Button:
            text: "Pressure Table Configuration"
            on_press:
                root.manager.transition.direction = 'right'
                root.manager.transition.duration = 1
                root.manager.current = 'screen_two'
        Button:
            text: "Write Config"
            on_press:
